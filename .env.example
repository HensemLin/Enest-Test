# ===========================================
# REQUIRED: You must set these values
# ===========================================

# OpenRouter / LLM Configuration
# Get your API key from: https://openrouter.ai/
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=mistralai/devstral-2512:free
EMBEDDING_MODEL=qwen/qwen3-embedding-8b

# Frontend-Backend Authentication
# ⚠️ DO NOT manually generate this key!
# Follow these steps:
#   1. Start backend: docker-compose up db api
#   2. Generate key: curl -X POST http://localhost:8000/api/keys/
#   3. Copy the "apiKey" from response
#   4. Paste it here as API_KEY
# See README.md "Step 4: Generate API Key" for details
API_KEY=YOUR_GENERATED_KEY_HERE

# Frontend API URL (used server-side for proxying)
# For local dev: http://localhost:8000
# For Docker: Set in docker-compose.yml as http://api:8000
API_URL=http://localhost:8000

# ===========================================
# OPTIONAL: Default values work for most cases
# ===========================================

# Database Configuration
# For Docker: Use DATABASE_HOSTNAME=db
# For local: Use DATABASE_HOSTNAME=localhost
DATABASE_HOSTNAME=localhost
DATABASE_PORT=5432
DATABASE_NAME=enest
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Memory Settings
MEMORY_MAX_TOKENS=2000
MEMORY_BUFFER_MESSAGES=10
MEMORY_SUMMARY_TRIGGER=15
SEMANTIC_MEMORY_TOP_K=5

# File Storage Paths
STORAGE_BASE_PATH=storage
PDF_STORAGE_PATH=storage/pdfs
VECTOR_STORAGE_PATH=storage/vectors
EXPORT_STORAGE_PATH=storage/exports
PROCESSED_STORAGE_PATH=storage/processed

# Upload Settings
MAX_UPLOAD_SIZE_MB=100